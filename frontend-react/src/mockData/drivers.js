// MOCK DATA — Phase 2 placeholder only.
// No real driver-dispatch backend exists yet.
// Replace by implementing GET /api/rides/:groupId/driver on the backend.
//
// Deliberately field-name placeholders ("Driver Name", "Rating", ...) rather
// than realistic-looking fake values — so it's obvious at a glance that
// nothing here is real data yet.
export const PLACEHOLDER_DRIVER = {
  name: 'Driver Name',
  rating: 'Rating',
  vehicle: 'Vehicle Model',
  plate: 'Vehicle Number',
  etaMinutes: 'ETA',
};
