# Backend URL the Vite dev server proxies /api requests to.
VITE_BACKEND_URL=http://localhost:4000

# Must match the backend's ALLOWED_EMAIL_DOMAIN (backend/.env.example).
VITE_ALLOWED_EMAIL_DOMAIN=spit.ac.in
